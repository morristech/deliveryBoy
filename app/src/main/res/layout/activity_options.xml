<?xml version="1.0" encoding="utf-8"?>
<LinearLayout
    android:id="@+id/activity_options"
    xmlns:android="http://schemas.android.com/apk/res/android"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:orientation="vertical">

    <include
        android:id="@+id/toolbar"
        layout="@layout/toolbar"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"/>

    <FrameLayout
        android:layout_width="match_parent"
        android:layout_height="0dp"
        android:layout_weight="1">

        <com.joanzapata.iconify.widget.IconButton
            android:id="@+id/accept_orders_button"
            android:layout_width="200dp"
            android:layout_height="200dp"
            android:layout_gravity="center"
            android:gravity="center"
            android:text="{fa-check-circle @android:color/holo_green_dark 60sp}\n\nAccept Orders"
            android:textColor="@android:color/black"
            android:textSize="20sp"/>
    </FrameLayout>

    <FrameLayout
        android:layout_width="match_parent"
        android:layout_height="0dp"
        android:layout_weight="1">

        <com.joanzapata.iconify.widget.IconButton
            android:id="@+id/deliver_orders_button"
            android:layout_width="200dp"
            android:layout_height="200dp"
            android:layout_gravity="center"
            android:gravity="center"
            android:text="{fa-cube @color/colorAccent 60sp }\n\nDeliver Orders"
            android:textColor="@android:color/black"
            android:textSize="20sp"/>

    </FrameLayout>

</LinearLayout>
